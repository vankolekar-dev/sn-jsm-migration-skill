# SN → JSM Migration Skill — Python Dependencies
# All dependencies use Python stdlib as fallback (no hard requirements)
# Install with: pip install -r requirements.txt

requests>=2.28.0    # Optional: faster HTTP than urllib (fallback built-in)
